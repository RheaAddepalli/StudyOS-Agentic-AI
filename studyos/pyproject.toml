[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "studyos"
version = "0.1.0"
description = "Autonomous learning-path and resource-discovery agent"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
